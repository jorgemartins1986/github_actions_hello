name: Hello GitHub Actions

on:
  push:
    branches:
      -main

jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Say hello
        run: echo "Hello, GitHub Actions!"
